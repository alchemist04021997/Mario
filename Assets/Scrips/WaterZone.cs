using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WaterZone : MonoBehaviour
{
    //float waterZoneX = 20, waterZoneY = 20;
    //private void Awake()
    //{
    //    waterZoneX += transform.position.x;
    //    waterZoneY += transform.position.y;
    //}
    //public bool IsInWater(Vector2 position)
    //{
    //    return position.x > transform.position.x && position.x < waterZoneX && position.y > transform.position.y && position.y < waterZoneY;
    //}
}